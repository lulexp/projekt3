#pragma once

#include "Command.h"
#include "UnaryCommand.h"
#include "BinaryCommand.h"
#include "TernaryCommand.h"
#include <string>
#include <vector>

using namespace std;

class Parser {

public:
	Parser(); 

	Parser(string* readiness_sigil);



	Command* parseCommand(string line);

private: 
	string* readiness_sigil;

	vector<string> tokenize(string line);

	Command* parseNoArguments(vector<string> tokens);

	UnaryCommand* parseUnary(vector<string> tokens);

	BinaryCommand* parseBinary(vector<string> tokens);

	TernaryCommand* parseTernary(vector<string> tokens);
};
